apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-app
          image: reonbritto/gitops01:f766418fabb0b019ecca499bbfd93c12c23f1e93
          imagePullPolicy: Always
          ports:
            - containerPort: 80
